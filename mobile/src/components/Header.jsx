import React from 'react';
import { SafeAreaView, Text } from 'react-native';

function Header() {
    return (
        <SafeAreaView>
            <Text>Header</Text>
        </SafeAreaView>
    );
}

export default Header;